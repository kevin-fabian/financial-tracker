package com.fabiankevin.app.models.enums;

public enum JointSpaceType {
   SHARE, // Share my resources
   RECIPIENT, // Ask recipient to share their asked resources
   BIDIRECTIONAL_SHARING, // Both parties share resources
}